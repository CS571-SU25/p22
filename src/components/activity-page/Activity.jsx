
// TODO: Build out activity information to be displayed for each activity

export default function Activity(props) {
    return (
        <div>
            <h1>Hello {props.name}</h1>
        </div>
    )
}